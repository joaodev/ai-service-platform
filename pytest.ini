[pytest]
asyncio_mode = auto
testpaths =
    tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast unit-level coverage for services and persistence behavior
    integration: API and database integration coverage
    ai: AI and agent behavior with mocked external providers